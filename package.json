{
  "name": "arc4-lib",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "scripts": {
    "build-minify": "npx -r fast-glob -r fs-extra -r uglify-es -r ts-node ts-node ./test/script/minify",
    "build-seed": "npx ts-node ./test/script/seed.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "expect": "^23.6.0",
    "ts-type": "^1.1.3"
  }
}
